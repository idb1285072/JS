const numbers = [1, 2, 3, 4];
console.log(numbers.unshift(100));
console.log(numbers.unshift(55, 66));
console.log(numbers);

// add the new elements to the start of the array
// return new array length